using UnityEngine;

namespace Hexagon
{
    public class Logger
    {
        public static void Log(string Message)
        {
            //if (GameManager.Instance.settings.logsEnabled) Debug.Log(Message);
        }
    }
}
